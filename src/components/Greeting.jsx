function Greeting({ message }) {
  return (
    <section className="card-surface greeting">
      <h1>{message}</h1>
      <p>Welcome to our React component lab.</p>
    </section>
  )
}

export default Greeting
